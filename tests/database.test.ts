import assert from 'node:assert/strict';import test from 'node:test';import {DatabaseSync} from 'node:sqlite';import {mkdtempSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {sqliteSchema} from '../app/sqlite-schema.ts';
test('products, fees and orders survive a database restart',()=>{const dir=mkdtempSync(join(tmpdir(),'nastia-'));const path=join(dir,'store.sqlite');let db=new DatabaseSync(path);db.exec(sqliteSchema);db.prepare('INSERT INTO products VALUES(?,?,?,?,?,?,?,?,?,?)').run('p1','منتج','Brand','فئة',1000,'وصف','["وردي"]','["30 مل"]',3,'#fff');db.prepare('INSERT INTO delivery_fees VALUES(?,?)').run('بغداد',5000);db.prepare(`INSERT INTO orders(customer_name,phone,province,city,district,street,landmark,notes,subtotal,delivery_fee,total) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).run('سارة محمد','07701234567','بغداد','بغداد','المنصور','14','مكتبة','',1000,5000,6000);db.close();db=new DatabaseSync(path);assert.equal((db.prepare('SELECT COUNT(*) count FROM products').get() as {count:number}).count,1);assert.equal((db.prepare('SELECT fee FROM delivery_fees WHERE province=?').get('بغداد') as {fee:number}).fee,5000);assert.equal((db.prepare('SELECT phone FROM orders').get() as {phone:string}).phone,'07701234567');db.close();rmSync(dir,{recursive:true,force:true})});
