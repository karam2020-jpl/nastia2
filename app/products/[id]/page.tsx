'use client';
import ProductGallery from '../../product-gallery';
import {notFound} from 'next/navigation';import {useState,use} from 'react';import {Header,Footer} from '../../components';import {money,Product as ProductType} from '../../data';import {useStore} from '../../providers';import {ShoppingBag} from 'lucide-react';
export default function Detail({params}:{params:Promise<{id:string}>}){const {id}=use(params);const {products,catalogStatus,refreshCatalog}=useStore();if(catalogStatus==='loading')return <><Header/><main className="container empty"><h2>جارٍ تحميل المنتج…</h2></main></>;if(catalogStatus==='error')return <><Header/><main className="container empty"><h2>تعذر تحميل المنتج</h2><p>تحققي من الاتصال ثم حاولي مجددًا.</p><button className="btn" onClick={()=>void refreshCatalog().catch(()=>{})}>إعادة المحاولة</button></main></>;const product=products.find((item)=>item.id===id);if(!product)return notFound();return <Product p={product}/>}
function Product({p}:{p:ProductType}){const [shade,setShade]=useState(p.shades[0]);const [size,setSize]=useState(p.sizes[0]);const [qty,setQty]=useState(1);const [message,setMessage]=useState('');const {add}=useStore();const submit=()=>{const result=add(p,shade,size,qty);setMessage(result.added===qty?`تمت إضافة ${qty} إلى السلة`:result.error||'تعذرت الإضافة')};return <><Header/><main className="container detail"><ProductGallery product={p}/><div><span className="brand">{p.brand} • {p.category}</span><h1>{p.name}</h1><h2 className="price">{money(p.price)}</h2><p>{p.description}</p><b>الدرجة</b><div className="choices">{p.shades.map((x)=><button key={x} onClick={()=>setShade(x)} className={'choice '+(shade===x?'active':'')}>{x}</button>)}</div><b>الحجم</b><div className="choices">{p.sizes.map((x)=><button key={x} onClick={()=>setSize(x)} className={'choice '+(size===x?'active':'')}>{x}</button>)}</div><div className="quantity"><button onClick={()=>setQty(Math.min(p.stock,qty+1))}>+</button><b>{qty}</b><button onClick={()=>setQty(Math.max(1,qty-1))}>−</button></div><button className="btn" disabled={!p.stock} onClick={submit}><ShoppingBag size={18}/> أضيفي للسلة</button>{message&&<p role="status" style={{color:'var(--green)'}}>{message}</p>}</div></main><Footer/></>}

