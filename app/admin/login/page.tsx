'use client';
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const data=new FormData(event.currentTarget);const response=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:data.get('username'),password:data.get('password')})});setBusy(false);if(!response.ok){setError((await response.json()).error);return}router.replace('/admin');router.refresh()}return <main className="login-page"><form className="formbox login-card" onSubmit={submit}><div className="logo">Nastia Beauty</div><h1>دخول الإدارة</h1><p className="muted">منطقة محمية لإدارة المتجر.</p>{error&&<p role="alert" className="notice">{error}</p>}<label className="field"><span>اسم المستخدم</span><input name="username" autoComplete="username" required/></label><label className="field"><span>كلمة المرور</span><input name="password" type="password" autoComplete="current-password" required/></label><button className="btn" disabled={busy}>{busy?'جارٍ التحقق…':'تسجيل الدخول'}</button></form></main>}
