export const sqliteSchema=`PRAGMA journal_mode=WAL; PRAGMA foreign_keys=ON;
CREATE TABLE IF NOT EXISTS app_meta(key TEXT PRIMARY KEY,value TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS products(id TEXT PRIMARY KEY,name TEXT NOT NULL,brand TEXT NOT NULL,category TEXT NOT NULL,price INTEGER NOT NULL CHECK(price>=0),description TEXT NOT NULL,shades TEXT NOT NULL,sizes TEXT NOT NULL,stock INTEGER NOT NULL CHECK(stock>=0),color TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS delivery_fees(province TEXT PRIMARY KEY,fee INTEGER NOT NULL CHECK(fee>=0));
CREATE TABLE IF NOT EXISTS orders(id INTEGER PRIMARY KEY AUTOINCREMENT,customer_name TEXT NOT NULL,phone TEXT NOT NULL,province TEXT NOT NULL,city TEXT NOT NULL,district TEXT NOT NULL,block TEXT,street TEXT NOT NULL,building TEXT,apartment TEXT,landmark TEXT NOT NULL,notes TEXT,status TEXT NOT NULL DEFAULT 'جديد',subtotal INTEGER NOT NULL,delivery_fee INTEGER NOT NULL,total INTEGER NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS order_items(id INTEGER PRIMARY KEY AUTOINCREMENT,order_id INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,product_id TEXT NOT NULL,product_name TEXT NOT NULL,brand TEXT NOT NULL,shade TEXT NOT NULL,size TEXT NOT NULL,quantity INTEGER NOT NULL,unit_price INTEGER NOT NULL);`;
